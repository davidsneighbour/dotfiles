{
  "extends": "@dnbhq/tsconfig/strict",
  // @todo move included files into strict config
  "include": ["**/*.ts", "**/*.tsx", "**/*.js", "lint-staged.config.ts"],
  // @todo rework the excluded files list
  "exclude": [
    "node_modules",
    "**/node_modules/*",
    "configs/theme/Dracula",
    "modules/protected",
    "containers/hal2025",
    "configs/vscode"
  ]
}
