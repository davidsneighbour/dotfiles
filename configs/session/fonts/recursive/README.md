# Recursive

Recursive is used as the UI and content typeface for the dotfiles desktop system.

Only `Recursive_VF_1.085.ttf` is retained. The separate static fonts and static collection files are intentionally removed because the current setup needs the Sans Linear axis from the variable font only.

The upstream license is kept in `LICENSE.txt`.
