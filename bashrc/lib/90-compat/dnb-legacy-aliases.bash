# shellcheck shell=bash

# Compatibility aliases for older scripts. Remove once all callers migrated.
# find usages:
# `searchfor __dnb_check_requirements --path /path/to/check`
#
# aliasing via
# __dnb_error() { dnb_error "$@"; }
