{
  "type": "module",
  "scripts": {
    "cleanup-logs": "node ./cleanup-logs.ts --config /home/patrick/.config/log-cleanup/config.toml",
    "cleanup-logs:dry": "node ./cleanup-logs.ts --config /home/patrick/.config/log-cleanup/config.toml --dry-run --verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.3.4"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}