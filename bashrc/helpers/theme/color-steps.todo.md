# todo list for color-steps.md

* [ ] explain science behind the decisions
* [ ] output format
  * [ ] return only the colors in a list
  * [ ] return the colors translated into rgb, hex, etc
  * [ ] return the colors as CSS variables (default)
  * [ ] return the colors as JSON object
  * [ ] return the colors with a string template per line
  * [ ] return as Obsidian `palette` format
* [ ] add more examples of how to use the output
