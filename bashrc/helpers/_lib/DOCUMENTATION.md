# `_lib/` documentation

This file documents every file currently present in `bashrc/helpers/_lib`.

Parent index: [`../INDEX.md`](../INDEX.md).

## Files

### `_lib/node.ts`

Shared TypeScript helper exporting ensureNodeVersion(requiredMajor), which exits if process.versions.node is below the required major.

Functions/methods defined:

* `ensureNodeVersion`
