{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/schemas/log-cleanup.config.schema.json",
  "title": "Log cleanup TOML config",
  "type": "object",
  "additionalProperties": false,
  "required": ["default"],
  "properties": {
    "default": {
      "$ref": "#/$defs/policy"
    },
    "rule": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/rule"
      },
      "default": []
    },
    "location": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/location"
      },
      "default": []
    }
  },
  "$defs": {
    "action": {
      "type": "string",
      "enum": ["delete", "compress"]
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["action", "keep_hours"],
      "properties": {
        "action": {
          "$ref": "#/$defs/action"
        },
        "keep_hours": {
          "type": "number",
          "minimum": 0
        }
      }
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["folderslug", "action", "keep_hours"],
      "properties": {
        "folderslug": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?!.*\\.\\.)[^\\\\]+$"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "keep_hours": {
          "type": "number",
          "minimum": 0
        }
      }
    },
    "location": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "root", "action", "keep_hours"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "root": {
          "type": "string",
          "minLength": 1
        },
        "pattern": {
          "type": "string",
          "minLength": 1,
          "default": "*"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "keep_hours": {
          "type": "number",
          "minimum": 0
        },
        "protected": {
          "type": "boolean",
          "default": false
        },
        "max_depth": {
          "type": "integer",
          "minimum": 0,
          "default": 2
        }
      }
    }
  }
}
