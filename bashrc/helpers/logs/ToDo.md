# ToDo

* adding archive rotation (e.g. delete .tar.xz after N days)
* once `theme-dracula` in config.toml has been confirmed disposable, flip `protected = false` (or drop the guard and run with `--allow-protected`) so it actually gets cleaned up
* consider adding more `[[location]]` entries for other large local-only payloads as they're identified (caches, other downloaded assets)
