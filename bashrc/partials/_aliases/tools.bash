#!/bin/bash
# shellcheck shell=bash
# dnb-dotfiles 3003.2.0

# loads a local codex profile, with optional verbose output and health checks
# alias codex="codex-local"
alias codex-update="npm install --global @openai/codex@latest"
