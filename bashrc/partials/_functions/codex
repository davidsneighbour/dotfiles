#!/bin/bash
# shellcheck shell=bash

codex-local() {
  if ! curl -s http://localhost:11434/v1/models >/dev/null 2>&1; then
    echo "ERROR: Ollama not running on localhost:11434" >&2
    return 1
  fi

  codex --profile local-ollama "$@"
}
