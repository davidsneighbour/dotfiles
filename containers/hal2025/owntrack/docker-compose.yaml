services:
  owntracks-recorder:
    image: owntracks/recorder:latest
    container_name: owntracks-recorder
    restart: unless-stopped
    ports:
      - "8083:8083"
    environment:
      - OTR_PORT=0
    volumes:
      - ./store:/store
      - ./config:/config
