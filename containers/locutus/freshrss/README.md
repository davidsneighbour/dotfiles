# FreshRSS Setup && Tailwind Theme Development

Start FreshRSS:

```bash
docker compose up -d
```

Stop FreshRSS:

```bash
docker compose down
```

Update:

```bash
docker compose down
docker compose pull
docker compose up -d
```

Logs (optional):

```bash
docker compose logs -f
```
